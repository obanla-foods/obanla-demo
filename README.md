Obanla Demo

This repository contains a static pitch deck site. The site will be deployed to GitHub Pages using the workflow in `.github/workflows/deploy.yml` when changes are pushed to the `main` branch.

After the workflow runs, the site will be available at:

https://obanla-foods.github.io/obanla-demo/

If the site doesn't appear immediately, visit the repository Settings → Pages to confirm the deployment.
